local Class = module(GetCurrentResourceName(), 'utils/class')

local Player = Class.new(function(self, level, xp)
    self.level = level
    self.xp = xp
end)

function Player:setNewJob

function Player:addExperience(job, xp)

end)

function Player:getExperience(job)

end)

function Player:getCurrentLevel()

end)